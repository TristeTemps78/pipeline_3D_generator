__pycache__/
*.pyc
renders/*.blend1
renders/*.blend@

# Réfs images © (locales seulement — décision de commit = utilisateur)
references/krokmou_ortho_side.png
references/krokmou_ref_front34.png
references/krokmou_anatomy_skeleton.png
references/krokmou_silh_trace.txt
references/krokmou_ortho_side_body.png

# Sequences PNG d'animation : lourdes, regenerables en une commande. Seuls les clips
# encodes (MP4/GIF pour la wyverne ; WEBM VP9 + render.json pour le dragon) sont committes
# — c'est eux le livrable pour le jeu aval.
renders/anim/*/
# ... mais le dragon (b30) niche ses clips dans renders/anim/dragon/ : on re-inclut ce
# dossier et on n'ignore QUE ses sous-dossiers de frames (idle/, fly/, ...).
!renders/anim/dragon/
renders/anim/dragon/*/
